default:
  suites:
    default:
      contexts:
        - FeatureContext
      paths:
        - '%paths.base%/tests/features/sprint-1'
  autoload:
    '': '%paths.base%/tests/php/contexts'

# You might want to add more profiles, e.g., for different environments or test types
# local:
#   formatter:
#     name: progress
#   extensions:
#     Behat\MinkExtension:
#       base_url: http://localhost:8000 # Example if using Mink
#       sessions:
#         default:
#           selenium2: ~ # Or other browser driver

formatters:
  progress: ~

# bootstrap: tests/features/bootstrap # Path relative to paths.base (which is .cursor) 